- name: Check role block
  block:
    - name: Execute check role
      ansible.builtin.include_role:
        name: "{{ item }}"
  rescue:
    - name: Record check result
      ansible.builtin.set_fact:
        _checks_results: "{{ (_checks_results | default([])) + [ansible_failed_result] }}"
