---
- name: Create PostgreSQL storage directory
  ansible.builtin.file:
    path: "{{ postgresql_data_dir }}"
    state: directory
    owner: "{{ foremanctl_user }}"
    group: "{{ foremanctl_group }}"
    mode: "0700"
    setype: container_var_lib_t

- name: Set PostgreSQL directory ownership for container UID/GID
  ansible.builtin.shell: |
    cd /tmp
    sudo -u {{ foremanctl_user }} XDG_RUNTIME_DIR={{ foremanctl_xdg_runtime_dir }} \
      podman unshare chown -R {{ postgresql_container_uid }}:{{ postgresql_container_gid }} {{ postgresql_data_dir }}
  args:
    executable: /bin/bash
  changed_when: true

- name: Create PostgreSQL socket directory
  ansible.builtin.file:
    path: "{{ postgresql_socket_dir }}"
    state: directory
    owner: "{{ foremanctl_user }}"
    group: "{{ foremanctl_group }}"
    mode: "0777"

- name: Set PostgreSQL environment variables
  ansible.builtin.set_fact:
    _postgresql_env:
      POSTGRESQL_MAX_CONNECTIONS: "{{ postgresql_max_connections }}"
      POSTGRESQL_SHARED_BUFFERS: "{{ postgresql_shared_buffers }}"
      POSTGRESQL_EFFECTIVE_CACHE_SIZE: "{{ postgresql_effective_cache_size }}"
      POSTGRESQL_LISTEN_ADDRESSES: "*"

- name: Configure PostgreSQL container as rootless user
  become: true
  become_method: "{{ foremanctl_become_method }}"
  become_user: "{{ foremanctl_user }}"
  environment: "{{ foremanctl_runtime_env }}"
  block:
    - name: Pull PostgreSQL container image
      containers.podman.podman_image:
        name: "{{ postgresql_container_image }}:{{ postgresql_container_tag }}"
        state: present

    - name: Create Podman secret for PostgreSQL admin password
      containers.podman.podman_secret:
        name: postgresql-admin-password
        data: "{{ postgresql_admin_password }}"
      notify:
        - Restart postgresql

    - name: Deploy PostgreSQL container
      containers.podman.podman_container:
        name: "{{ postgresql_container_name }}"
        image: "{{ postgresql_container_image }}:{{ postgresql_container_tag }}"
        state: quadlet
        healthcheck: pg_isready
        sdnotify: healthy
        network: "{{ postgresql_network }}"
        volumes:
          - "{{ postgresql_data_dir }}:/var/lib/pgsql/data:z"
          - "{{ postgresql_socket_dir }}:/tmp/socket:z"
        secrets:
          - 'postgresql-admin-password,target=POSTGRESQL_ADMIN_PASSWORD,type=env'
        env: "{{ _postgresql_env }}"
        quadlet_options:
          - |
            [Install]
            WantedBy=default.target foreman.target
            [Unit]
            PartOf=foreman.target

    - name: Run daemon reload
      ansible.builtin.systemd:
        daemon_reload: true
        scope: user

    - name: Flush handlers to restart services
      ansible.builtin.meta: flush_handlers

    - name: Start the PostgreSQL Service
      ansible.builtin.systemd:
        name: "{{ postgresql_container_name }}"
        state: started
        scope: user

# On first startup PostgreSQL only listens on /var/run/postgresql inside the
# container; the bind-mounted socket dir is not yet in unix_socket_directories.
# Use podman exec to reach the container-internal socket and configure it.
# After the restart triggered by the notify, /tmp/socket (= postgresql_socket_dir
# on the host) becomes available so subsequent community.postgresql tasks can
# connect via the host-side socket path.
- name: Check current unix_socket_directories setting
  become: true
  become_method: "{{ foremanctl_become_method }}"
  become_user: "{{ foremanctl_user }}"
  environment: "{{ foremanctl_runtime_env }}"
  ansible.builtin.command:
    cmd: >-
      podman exec {{ postgresql_container_name }}
      psql -U postgres -tAc
      "SELECT setting FROM pg_settings WHERE name='unix_socket_directories'"
  register: postgresql_current_socket_dirs
  changed_when: false

- name: Configure unix_socket_directories to include the bind-mounted path
  when: "'/tmp/socket' not in postgresql_current_socket_dirs.stdout"
  become: true
  become_method: "{{ foremanctl_become_method }}"
  become_user: "{{ foremanctl_user }}"
  environment: "{{ foremanctl_runtime_env }}"
  ansible.builtin.command:
    cmd: >-
      podman exec {{ postgresql_container_name }}
      psql -U postgres -c
      "ALTER SYSTEM SET unix_socket_directories = '/var/run/postgresql,/tmp/socket'"
  changed_when: true
  notify:
    - Restart postgresql

- name: Flush handlers to apply socket configuration
  ansible.builtin.meta: flush_handlers

- name: Wait for PostgreSQL container to be healthy
  become: true
  become_method: "{{ foremanctl_become_method }}"
  become_user: "{{ foremanctl_user }}"
  environment: "{{ foremanctl_runtime_env }}"
  ansible.builtin.command:
    cmd: podman healthcheck run {{ postgresql_container_name }}
  retries: 30
  delay: 2
  register: _postgresql_healthcheck
  until: _postgresql_healthcheck.rc == 0
  changed_when: false

# DEBUG: Show PostgreSQL configuration
- name: DEBUG - Show PostgreSQL listen_addresses
  become: true
  become_method: "{{ foremanctl_become_method }}"
  become_user: "{{ foremanctl_user }}"
  environment: "{{ foremanctl_runtime_env }}"
  ansible.builtin.command:
    cmd: >-
      podman exec {{ postgresql_container_name }}
      psql -U postgres -tAc "SHOW listen_addresses"
  register: _postgresql_listen_addresses
  changed_when: false

- name: DEBUG - Display listen_addresses
  ansible.builtin.debug:
    msg: "PostgreSQL listen_addresses: {{ _postgresql_listen_addresses.stdout }}"

- name: DEBUG - Show pg_hba.conf contents
  become: true
  become_method: "{{ foremanctl_become_method }}"
  become_user: "{{ foremanctl_user }}"
  environment: "{{ foremanctl_runtime_env }}"
  ansible.builtin.command:
    cmd: podman exec {{ postgresql_container_name }} cat /var/lib/pgsql/data/pg_hba.conf
  register: _postgresql_pg_hba_contents
  changed_when: false
  failed_when: false

- name: DEBUG - Display pg_hba.conf
  ansible.builtin.debug:
    msg: >-
      {{ _postgresql_pg_hba_contents.stdout_lines if _postgresql_pg_hba_contents.rc == 0
      else 'pg_hba.conf not found (may be managed differently by sclorg container)' }}

- name: DEBUG - Test TCP connection to PostgreSQL
  ansible.builtin.wait_for:
    host: localhost
    port: 5432
    timeout: 5
  register: _postgresql_tcp_test
  failed_when: false
  when: postgresql_network == "host"

- name: DEBUG - Display TCP connection test result
  ansible.builtin.debug:
    msg: "PostgreSQL TCP connection test: {{ 'SUCCESS' if _postgresql_tcp_test is successful else 'FAILED' }}"
  when: postgresql_network == "host"

# SCRAM-SHA-256 is default for PostgreSQL 14+,
# after the upgrade, we can drop this task.
- name: Use scram-sha-256 for password encryption
  community.postgresql.postgresql_set:
    login_user: postgres
    login_unix_socket: "{{ postgresql_socket_dir }}"
    name: password_encryption
    value: "scram-sha-256"
  notify:
    - Restart postgresql

- name: Flush handlers to apply password encryption change
  ansible.builtin.meta: flush_handlers

- name: Create PostgreSQL users
  community.postgresql.postgresql_user:
    name: "{{ item.name }}"
    password: "{{ item.password }}"
    login_user: postgres
    login_unix_socket: "{{ postgresql_socket_dir }}"
    role_attr_flags: "{{ item.role_attr_flags | default(omit) }}"
    state: present
  loop: "{{ postgresql_users }}"
  no_log: true

- name: Create PostgreSQL databases
  community.postgresql.postgresql_db:
    name: "{{ item.name }}"
    owner: "{{ item.owner }}"
    login_user: postgres
    login_unix_socket: "{{ postgresql_socket_dir }}"
    state: present
  loop: "{{ postgresql_databases }}"
