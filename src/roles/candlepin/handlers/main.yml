---
- name: Restart candlepin
  ansible.builtin.command:
    cmd: "systemctl --machine={{ foremanctl_user }}@ --user restart candlepin"
  changed_when: true
  # Alternative approach using systemd module (requires become_user):
  # become: true
  # become_user: "{{ foremanctl_user }}"
  # ansible.builtin.systemd:
  #   name: candlepin
  #   state: restarted
  #   scope: user
